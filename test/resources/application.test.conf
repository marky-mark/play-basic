kafka {
  bootstrap.servers = "localhost:5000"
  zookeeper.servers = "localhost:5001"

  producer.request.size = "102400000"

  internal-infos-events {
    topic = "unit-topic"
    consumer-group = "unit-topic-group"
  }
}
