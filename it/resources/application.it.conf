url=${?DOCKER_IP}

database {
  config.name = "database.postgres"

  postgres {
    profile = "services.slickbacked.ExtendedPostgresDriver$"
    db {
      url="jdbc:postgresql://"${DOCKER_IP}":5432/playbasic"
      user="playbasic"
      password="playbasic"
    }
  }
}

external-services {
  info-client {
    base-url = "http://"${DOCKER_IP}":9000"
  }
}


